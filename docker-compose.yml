version: "3.0"

services:
  hlds:
    build:
      context: .
      dockerfile: Dockerfile
		volumes:
		  - ./config/cstrike:/server/hlds_l/cstrike 
    ports:
      - 27015:27015
      - 27015:27015/udp
    command:
			- ./hlds_run -port 27015 +game cstrike +map de_dust2
